<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign in | Marketplace</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="auth">
  <main class="auth-card">
    <p class="eyebrow">Access</p>
    <h1>Sign in</h1>
    <p class="muted">Use your account to continue. New here? <a href="/auth/register">Create an account</a>.</p>
    <% if (error) { %>
      <div class="alert error"><%= error %></div>
    <% } %>
    <form class="stack" action="/auth/login" method="post">
      <label>Email<input required name="email" type="email" placeholder="you@example.com"></label>
      <label>Password<input required name="password" type="password" placeholder="������"></label>
      <button class="btn primary" type="submit">Sign in</button>
    </form>
  </main>
</body>
</html>
