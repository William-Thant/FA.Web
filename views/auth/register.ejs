<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create account | Marketplace</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="auth">
  <main class="auth-card">
    <p class="eyebrow">Join</p>
    <h1>Create your account</h1>
    <p class="muted">Register to rent cars and manage your bookings. Already have an account? <a href="/auth/login">Sign in</a>.</p>
    <% if (error) { %>
      <div class="alert error"><%= error %></div>
    <% } %>
    <form class="stack" action="/auth/register" method="post">
      <label>Name<input required name="name" placeholder="Avery Tan"></label>
      <label>Email<input required name="email" type="email" placeholder="you@example.com"></label>
      <label>Password<input required name="password" type="password" placeholder="Enter password"></label>
      <label>Account Type
        <select required name="role">
          <option value="">Select your role</option>
          <option value="buyer">Buyer</option>
          <option value="dealer">Dealer</option>
          <option value="admin">Admin</option>
        </select>
      </label>
      <button class="btn primary" type="submit">Create account</button>
    </form>
  </main>
</body>
</html>
