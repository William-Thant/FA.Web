<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <main class="error">
    <h1><%= message %></h1>
    <p class="muted">Status: <%= error.status || 500 %></p>
    <% if (error.stack) { %>
      <pre><%= error.stack %></pre>
    <% } %>
    <a class="btn primary" href="/">Back home</a>
  </main>
</body>
</html>
