<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DriveWay | Premium Car Rentals</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('partials/navbar', { navItems, search }) %>
  <main class="page">
    <section class="hero">
      <div>
        <p class="eyebrow">Drive smart · Drive now</p>
        <h1>Rent your next car with confidence.</h1>
        <p class="lede">Browse new releases, reliable used rides, and a modern electric lineup. Flexible daily pricing, transparent policies, and admin-grade fleet controls.</p>
        <div class="hero-actions">
          <a class="btn primary" href="#cars">Browse cars</a>
          <a class="btn ghost" href="/admin/cars">Admin panel</a>
        </div>
      </div>
      <div class="hero-card">
        <p class="label">Highlighted</p>
        <div class="highlight-grid">
          <% highlights.forEach(function(car){ %>
            <div class="card highlight">
              <div class="card-top">
                <span class="pill"><%= car.category %></span>
                <span class="status available"><%= car.condition %></span>
              </div>
              <h3><%= car.name %></h3>
              <p class="muted"><%= car.type %> · $<%= car.pricePerDay %>/day</p>
            </div>
          <% }) %>
        </div>
      </div>
    </section>

    <section class="category-strip" id="cars">
      <% navItems.slice(0,4).forEach(function(item){ %>
        <a href="<%= item.href %>" class="chip <%= category && category.toLowerCase() === item.label.toLowerCase() ? 'chip-active' : '' %>">
          <%= item.label %>
        </a>
      <% }) %>
    </section>

    <section class="car-grid">
      <% if (cars.length === 0) { %>
        <div class="empty">
          <p>No cars match your filters. Try a different category or clear the search bar.</p>
        </div>
      <% } %>
      <% cars.forEach(function(car){ %>
        <article class="card">
          <div class="card-top">
            <span class="pill"><%= car.category %></span>
            <span class="status available">Available</span>
          </div>
          <h3><%= car.name %></h3>
          <p class="muted"><%= car.type %> · <%= car.condition %></p>
          <p class="price">$<%= car.pricePerDay %><span>/day</span></p>
          <% if (currentUser) { %>
            <form action="/cars/<%= car.id %>/rent" method="post">
              <button class="btn primary" type="submit">Rent now</button>
            </form>
          <% } else { %>
            <a class="btn ghost" href="/auth/login">Sign in to rent</a>
          <% } %>
        </article>
      <% }) %>
    </section>

    <section class="info-grid" id="loan">
      <div class="info-card">
        <h3>Loan & Ownership</h3>
        <p>Explore loan pathways for long-term car ownership. Admins can flag vehicles eligible for purchase or longer leases.</p>
        <a class="text-link" href="#loan">Talk to us</a>
      </div>
      <div class="info-card" id="sell">
        <h3>Sell or Trade-in</h3>
        <p>Upgrade your fleet by listing existing cars. Admins can mark units as maintenance, rented, or ready to list.</p>
        <a class="text-link" href="#sell">List a car</a>
      </div>
      <div class="info-card" id="more">
        <h3>More services</h3>
        <p>Pick-up scheduling, insurance pairing, and route optimization help renters stay on time and on budget.</p>
        <a class="text-link" href="#more">View all</a>
      </div>
    </section>
  </main>
</body>
</html>
